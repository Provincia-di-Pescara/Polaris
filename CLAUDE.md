# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Sistema telematico di assegnazione di spazi sportivi pubblici (palestre scolastiche di competenza provinciale). Obiettivo: eliminare discrezionalità umana nell'assegnazione, sostituendola con regole matematiche deterministiche, tracciabili e riproducibili da terzi.

**Stato attuale**: analisi requisiti chiusa. Fase 1 (schema DB), Fase 2 (motore Go: calc + sorteggio + round-robin) e Fase 3 (persistenza Postgres + esposizione HTTP del motore) implementate, testate, verificate con Postgres reale. Fase 4 (Backend Node.js+TypeScript) in corso: scaffold, `GET /stagioni`, autenticazione locale backoffice **e** OIDC SPID/CIE pubblico (login/refresh/logout/rate-limit, entrambe, protezione login-CSRF inclusa), CRUD quadro disponibilità (6 entità), Flusso pubblico blocco 1/4 (accreditamento associazione + sub-deleghe gerarchiche + upload documenti) **e** blocco 2/4 (domanda con fabbisogni/preferenze/blocchi allenamento/giornate gara, verifica ammissibilità, pubblicazione esiti, osservazioni/riesame), parte admin completa (impostazioni OIDC via UI, CRUD utenti backoffice con invito email, parametrico versionato) — tutto verificato end-to-end con Postgres reale e IdP mock realistico. Fase 6 (Docker Compose + CI/CD) **quasi chiusa**: `docker-compose.yml`/`.override.yml` verificati con bring-up reale (postgres+engine+backend, 3 bug reali trovati e corretti — poi entrambi i frontend wired nel compose/release.yml, altri 3 bug reali trovati e corretti — poi migration spostate dall'entrypoint del container `backend` stesso invece che da CI, topologia di deploy reale chiarita col committente 2026-08-23: VM con Portainer + Traefik file-based, vedi `docs/claude/cicd-docker.md`), workflow GitHub Actions scritti (da verificare con un run reale, unico residuo di questa fase). Fatti anche: Sentry/GlitchTip opzionale sui 4 servizi applicativi (`SENTRY_DSN_*`, mai un requisito per girare — release/footer versione riusano `IMAGE_TAG`, vedi `docs/claude/backend-node.md`/`docs/RUNBOOK-deploy.md`), blocchi gara nel motore Go (Fase 6 normativa, art. B.12-15, end-to-end con VA iniziale nel round-robin), audit log applicativo (art. B.39) su login/logout, bootstrap primo admin via wizard email (SMTP di bootstrap da `.env`, endpoint `/auth/bootstrap/*` — **UI backoffice aggiunta il 2026-08-23**: `BootstrapView`/`BootstrapVerificaView`, mai esistita prima, gap reale scoperto al primo deploy su istanza vuota, nessun modo di raggiungerla), quota nuove associazioni (art. 12, parametro default 0), housekeeping/retention GDPR, backup Postgres con restore verificato. Frontend pubblico: login OIDC SPID/CIE, Header e sessione (persona autenticata + entità rappresentate via `GET /pubblico/deleghe/mie`) **e** AccreditamentoDelegaView (accreditamento nuova associazione + upload documento + sub-delega, selettore stagione in Header, estensione anagrafica con RASD/assicurazioni/referenti+DAE per `Associazioni_Documenti.docx`) **e** WizardDomandaView (presentazione domanda a 4 step: discipline/classe attività/squadre, fabbisogno minimo/ottimale con anteprima FR opzionale dal motore Go, blocco/i giornata gara, preferenze slot + blocchi allenamento) **e** EsitiIsfView (tabellone ISF pubblico + sezione "La mia domanda" con KPI FR/CP/ISF/slot assegnati calcolati da dati reali, osservazioni e richiesta di riesame per domande escluse) **e** ConcertazioneView (bollettino proposta provvisoria art. B.23, proposta/accettazione/annullamento scambi/cessioni/utilizzo slot libero tra associazioni art. B.24-28, simulazione ISF client-side informativa) **e** CalendarioDefinitivoView (griglia settimanale sola lettura sulla settimana tipo definitiva art. B.29-B.31, associazione propria evidenziata, badge tipo/efficacia convenzione) collegati alle API reali — vedi `docs/claude/oidc-spid-cie.md`/`docs/claude/backend-node.md`/`docs/claude/motore-go.md` — **frontend pubblico completo, tutte le 6 view collegate alle API reali**. Frontend backoffice: tutte le 6 view collegate alle API reali (Impianti/Spazi/Slot/Discipline/Istituzioni, Control Room/coda motore Go, Parametri Sistema, Deleghe & Accreditamenti, Audit & Sorteggio con verifica HMAC reale via SubtleCrypto, Statistiche con endpoint di aggregazione dedicato — vedi `docs/claude/backend-node.md`) — **UI Fase 5 lato backoffice chiusa al 100%**. Flusso pubblico blocco 3/4 (pubblicazione proposta provvisoria B.23 + concertazione tra associazioni B.24-28: scambi/cessioni/utilizzo slot libero, validazione FIFO+lock) **e** blocco 4/4 (riassegnazione finale B.29 + settimana tipo definitiva B.30-31: approvazione quadro, convenzioni, efficacia) fatti — **flusso pubblico completo (blocchi 1-4/4) chiuso**. Fase 15 (gestione stagionale post-`definitiva`) **chiusa (B.32-36 completa lato backend)**: indisponibilità sopravvenute (B.33, solo backoffice + lettura pubblica scoped) e variazioni ordinarie tra associazioni (B.32: liberazione/recupero/scambio_temporaneo/utilizzo_occasionale, con lock avviso per-occorrenza + indice unico anti-race, nessuna coda di validazione backoffice attiva — istruzione esplicita del committente) **più** rilevazione utilizzo effettivo (B.34, solo modalità registro impianto) ed escalation mancato utilizzo (B.35: ciclo di giustificazione a due fasi, coda soglie diffida/decadenza, decadenza che libera lo slot e congela la convenzione collegata — `registraUtilizzo` verifica esplicitamente titolarità mobile per-occorrenza e indisponibilità sopravvenute prima di imputare un mancato utilizzo, altrimenti un'associazione non più titolare rischierebbe una decadenza indebita). B.36 (effetti sui coefficienti stagione successiva) solo storico interrogabile, aggancio al calcolo CSD rimandato alla Fase 7 (taratura CSD, non ancora chiusa). Residui noti: provider SPID/CIE/eIDAS ancora hardcoded a 'spid', OIDC mai testato contro il vero pa-sso-proxy (verifica manuale in browser ancora pendente, tool di supporto `backend-node/scripts/mock-idp.mjs`). Segnalato dall'ufficio (2026-08-17, non ancora un requisito formalizzato): serviranno in futuro documenti/dati aggiuntivi oltre alla delega (es. matricola DAE) — DAE è legato all'impianto/struttura, non all'associazione (un'associazione con due impianti nello stesso giorno necessita due DAE), quindi il dato andrà su `impianti`, non su `associazioni_documenti`. Nessun campo introdotto, solo un promemoria. Residuo noto aperto dal blocco "estensione anagrafica associazioni" (2026-08-17): le 7 categorie di soggetti non-sportive (`tipologia_soggetto`) fanno solo "richieste spot" durante la stagione, un flusso di prenotazione diverso da quello stagionale (fabbisogno/ISF/round-robin) e non ancora specificato — nessun documento normativo lo descrive, nessun enforcement implementato (`POST /pubblico/domande` resta raggiungibile da tutti indipendentemente dalla categoria). Dettagli in `docs/superpowers/specs/2026-08-17-associazioni-documenti-estensione-design.md`. Residuo noto dal blocco ConcertazioneView (2026-08-19, trovato dalla final review): il form "Proponi nuova concertazione" per il tipo `utilizzo_slot_libero` può offrire solo slot già assegnati (le opzioni vengono dal bollettino `trovaPropostaProvvisoria`, che espone solo assegnazioni `provvisoria`/`validata`) perché non esiste ancora un endpoint che elenchi gli slot realmente liberi di una stagione — tali proposte tipicamente falliscono la validazione backoffice più avanti (`controlloAssegnazioneAttivaAttesa` rigetta "slot non è più libero"); la UI mostra solo un avviso informativo non bloccante, un vero endpoint di slot liberi resta un follow-up. Piano completo: `docs/SPEC.md`.

## Versioni target (verificate via web search, non da training data — ricontrollare a inizio di ogni fase nuova, l'ecosistema si muove in fretta)

- **PostgreSQL 18** (`postgres:18-alpine`). PostgreSQL 19 è in beta a luglio 2026, non da usare in produzione.
- **Go 1.26** (patch più recente, es. 1.26.5) per il motore algoritmico — Fase 2 completata.
- **Node.js 24** (Active LTS) per il backend API. Node 26 esiste già come release "Current" ma entra in LTS solo da ottobre 2026 — non usarlo prima per un sistema in produzione.
- **TypeScript 7.0** ovunque (backend Node e frontend), nessun blocco: **React 19.2** scelto come framework per entrambi i frontend (decisione presa proprio per evitare il vincolo Vue/Volar su TS7, non ancora supportato — atteso TS 7.1 ~ottobre 2026).

**Specifica di progetto**: `docs/SPEC.md` — piano completo di tutte le fasi (1–8), mappatura articolo-per-articolo verso Allegato B/Doc Principale, lacune note con priorità, decisioni aperte. Aggiornarla alla chiusura di ogni fase; questo file resta la memoria operativa fine-grained.

## Documenti di riferimento (fonte di verità normativa)

Cartella `documenti/`, formato .docx (leggere con estrazione XML via zipfile+regex, il tool Read nativo non gestisce binari .docx):

- `Documento_Principale_-_Sistema_Assegnazione_Spazi_Sportivi__Completo_.docx` — principi generali, fasi procedimento, tutela nuove associazioni, GDPR.
- `Allegato_A_-_Criteri_di_Assegnazione__Completo_.docx` — formule: fabbisogno riconosciuto (FR), indice di soddisfazione (ISF), coefficienti (CRS, CAA, CSD, CP).
- `Allegato_B_-_Procedura_Operativa__Completo_.docx` — procedura operativa passo-passo (16 fasi, artt. B.1-B.39): dall'accreditamento alla settimana tipo definitiva.

Ogni regola di business implementata deve essere riconducibile a un articolo preciso di questi tre documenti normativi. Non introdurre logiche non esplicitamente scritte (istruzione esplicita del committente).

**Nota su `Associazioni_Documenti.docx`**: documento non ufficiale fornito informalmente dal committente (2026-08-17) con l'elenco dei campi anagrafici/documenti/assicurativi del modulo di accreditamento associazione (tipologia soggetto, rappresentante legale/delegato, RASD/affiliazione, RCT/RCO, responsabili sicurezza/emergenze+DAE) — non conservato in `documenti/` (non è fonte normativa). Contenuto già recepito nello schema/form (blocco "estensione anagrafica associazioni") e interamente trascritto in `docs/superpowers/specs/2026-08-17-associazioni-documenti-estensione-design.md`, che resta il riferimento per eventuali chiarimenti futuri su quei campi.

**Allegato parametrico**: documento ufficiale dell'Ente con i valori numerici definitivi non ancora prodotto. Sviluppo procede con valori placeholder (marcati 🔺, dettagli in `docs/claude/parametrico-normativo.md`), versionati in DB e sostituibili dall'Ente in qualunque momento senza migrazione di schema — non blocca l'avvio di Fase 1/2.

## Indice argomenti (`docs/claude/`)

Questo file resta volutamente snello (overview + vincoli sempre validi). Il dettaglio operativo per ciascuna area è in file separati, da leggere quando si lavora in quell'area specifica — non caricati automaticamente ad ogni sessione:

- **`docs/claude/regole-calcolo.md`** — regole di calcolo consolidate (FR/ISF/CP, tolleranza parità ISF, fasce pregiate, CSD, sorteggio tracciato, terminazione round-robin, lock/concorrenza, audit log). **Lettura obbligatoria prima di toccare qualunque logica di calcolo**, sia in Go (`engine-go/`) sia in Node (`backend-node/`) — sono le regole di dominio che vincolano entrambi i layer.
- **`docs/claude/schema-db.md`** — schema Postgres (Fase 1): elenco migration, gotcha SQL (`num_nonnulls`, `EXCLUDE USING gist`, colonne `GENERATED`), note ambiente Windows/Git Bash/Docker, comandi di test locale.
- **`docs/claude/motore-go.md`** — motore algoritmico Go (Fase 2-3): package `internal/calc`/`sorteggio`/`roundrobin`/`istruttoria`/`gara`/`postgres`/`httpapi`, comandi Docker per build/test/integrazione, gotcha CI pnpm.
- **`docs/claude/backend-node.md`** — backend Node.js+TypeScript (Fase 4, la sezione più corposa): auth locale+OIDC, CRUD quadro disponibilità, flusso pubblico blocchi 1-4/4, Fase 15 (B.32-36), parametrico versionato, coda verso il motore Go, frontend backoffice collegato alle API reali (tutte le 6 view). Ogni blocco chiuso è documentato con bug reali trovati, gotcha di test, pattern riusabili.
- **`docs/claude/oidc-spid-cie.md`** — integrazione OIDC SPID/CIE (pa-sso-proxy): PKCE, verifica JWKS, login-CSRF fix, bug reali trovati dallo smoke test, cosa resta da verificare contro il proxy reale.
- **`docs/claude/cicd-docker.md`** — Docker Compose (prod/dev) e GitHub Actions CI/CD: bug reali trovati con bring-up reale, gotcha Postgres 18 volume, `.dockerignore`, namespace GHCR lowercase.
- **`docs/claude/parametrico-normativo.md`** — valori di default dell'allegato parametrico (placeholder 🔺 da validare con l'Ente), struttura CSD, specifica esatta del verbale di sorteggio tracciato (art. B.38).

## Architettura target (5 container)

1. **DB — PostgreSQL.** Single source of truth. Vincoli relazionali rigidi, exclusion constraint contro sovrapposizioni slot, lock transazionali per gestire concorrenza in fase di concertazione. Parametri di sistema (🔧, vedi `docs/claude/regole-calcolo.md`) in tabella `allegato_parametrico` versionata, editabile da admin via UI backoffice — mai hardcoded, mai letta "current" da un'elaborazione storica (ogni run referenzia la versione vigente al momento dell'esecuzione).
2. **Motore algoritmico — Go.** Microservizio puro, isolato: solo calcolo (FR/ISF/CP, ordine esame fasce, loop round-robin, sorteggio tracciato). Nessuna dipendenza da HTTP/auth/CRUD — deve restare testabile in isolamento per garantire determinismo e riproducibilità bit-esatta (requisito esplicito e ripetuto nei documenti: art. 28 Doc Principale, art. B.1 Allegato B).
3. **Backend API/Backoffice — Node.js + TypeScript.** Autenticazione OIDC (SPID/CIE) per frontend pubblico, autenticazione locale per frontend admin, validazione, CRUD, orchestrazione delle fasi procedurali, coda verso il motore Go. Riusa le regole di business del motore Go via RPC — non duplicare logica di calcolo in Node.
4. **Frontend pubblico — React 19 + TypeScript 7.** Accesso associazioni (e scuole, che seguono iter di delega manuale) via SPID/CIE/eIDAS. Richiesta delega/abilitazione per una o più associazioni, domanda, preferenze, concertazione, calendario.
5. **Frontend admin (backoffice provincia) — React 19 + TypeScript 7.** Login locale (no OIDC). Primo avvio: wizard di seeding SMTP + creazione primo account admin con validazione via link email (niente credenziali in `.env`). Due ruoli: **admin** (tutto, incluse impostazioni/parametri: SMTP, OIDC, parametri di sistema, loghi, utenti backoffice) e **operatore** (operatività pratica: deleghe, CRUD palestre/slot, istruttoria — non impostazioni/parametri).

Infrastruttura: Docker, CI/CD via GitHub Actions → GHCR, reverse proxy davanti ai frontend/API.

## Workflow di sviluppo

Solo io e il committente su questo progetto: **lavoro diretto su `master`, mai PR**. Worktree/branch temporanei per isolamento durante l'implementazione vanno bene, ma l'integrazione finale è sempre merge diretto (fast-forward quando possibile) su `master`, mai una pull request da rivedere.

## Vincoli progettuali non negoziabili

- **Determinismo**: stesso input → stesso output, sempre. Vietato usare fonti di non-determinismo non seedate (orologio di sistema, ordine di iterazione di map non ordinate, float non specificato) nel motore Go.
- **Sorteggio tracciato**: seme pubblicato prima dell'elaborazione, algoritmo deterministico e documentato pubblicamente, verbale automatico, esito riproducibile da terzi.
- **Tracciabilità**: ogni operazione registrata con persona fisica, associazione rappresentata, ruolo, data/ora (art. 53 Doc Principale, art. B.39 Allegato B).
- **Unità di misura**: tutti i conteggi rilevanti (fabbisogno, valore assegnato, limiti di concentrazione) sono espressi in minuti, mai in numero di slot (le fasce hanno durate diverse).
- **Denaro**: i corrispettivi per l'uso delle palestre non transitano mai dalla piattaforma (regolati direttamente tra associazioni e istituzioni scolastiche).
