// Placeholder per sviluppo locale (pnpm dev) — in produzione questo file viene
// rigenerato dall'entrypoint nginx da SENTRY_DSN/APP_VERSION del container
// all'avvio (mai a build time: la stessa immagine gira su più istanze).
window.__SENTRY_DSN__ = '';
window.__APP_VERSION__ = '';
